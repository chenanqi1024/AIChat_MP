# 本次改动日志

## 新增文件
- `logs/2026-07-05-agents-md.md`：记录本次 `AGENTS.md` 文档更新内容。

## 修改文件
- `AGENTS.md`：补充微信小程序工程结构、页面新增规则、编码规范、Figma Make MCP 读取流程和文档任务验证要求。

## 完成功能
- 明确本项目为原生微信小程序结构，页面需按 `.js`、`.json`、`.wxml`、`.wxss` 成组维护。
- 明确新增页面必须注册到 `app.json`。
- 明确 Figma 和 `figma.com/make` 链接必须通过 Figma MCP 读取，不通过浏览器抓取或截图猜测。
- 明确 Figma MCP 输出只作为设计参考，需要翻译为 WXML/WXSS/JS。
- 明确 MCP 返回资源的使用规则，避免占位图和不必要的新依赖。

## 当前仍未完成的内容
- 本次仅更新项目协作说明，未实现具体小程序页面或接口功能。

## 运行说明或注意事项
- 本次为文档变更，无需运行微信开发者工具编译。
- 后续涉及 Figma 设计实现时，应先通过 Figma MCP 获取 `get_design_context`，必要时补充 `get_metadata` 和 `get_screenshot`。
