# AGENTS.md

## 项目名称
AI 陪伴聊天 App 小程序版

## 项目目标
实现一款 AI 陪伴聊天微信小程序。

本项目以 Figma 设计稿为视觉与交互参考，以后端 API 文档为接口实现依据，使用微信小程序原生能力与 JavaScript 完成可运行的小程序版本。

---

## 技术约束
- 目标平台为微信小程序
- 使用原生微信小程序 + JavaScript 开发
- 样式使用 WXSS
- 页面尺寸优先使用 `rpx`，并处理安全区与不同机型适配
- 网络请求使用小程序原生请求能力封装
- 如需聊天流式返回，优先采用后端可支持的小程序可用方案
- 除非确有必要，不要引入额外第三方库
- 所有实现应尽量简单、清晰、可读
- 不要过度设计，不要过度封装
- 优先保证代码可维护性、可运行性、可调试性
- 使用微信开发者工具进行编译与预览验证，必要时补充真机验证

---

## 当前工程结构
- `app.js`：小程序全局生命周期与全局数据
- `app.json`：全局页面注册、窗口配置、基础能力配置
- `app.wxss`：全局样式
- `pages/`：页面目录，每个页面使用同名 `.js`、`.json`、`.wxml`、`.wxss` 文件成组维护
- `utils/`：跨页面复用的工具方法
- `project.config.json`：微信开发者工具项目配置
- `project.private.config.json`：本机私有配置，除非任务明确要求，不要修改

新增页面时必须：
- 在 `pages/` 下创建独立页面目录
- 同步创建页面所需的 `.js`、`.json`、`.wxml`、`.wxss`
- 在 `app.json` 的 `pages` 中注册页面路径
- 确认首屏、导航、滚动区域和安全区表现正常

---

## 设计与实现依据

后端 API 文档：
file:///Users/chenanqi/Desktop/AIChat-API/Doc/API.html#history
Figma 设计稿：
https://www.figma.com/make/rad7azsHRGU10Q4xfgLz7Z/AI%E9%99%AA%E4%BC%B4%E8%81%8A%E5%A4%A9App%E5%BC%95%E5%AF%BC%E9%A1%B5--Community-?t=m7Wrzv1O5l96MVBp-1

## Figma 实现要求
- 严格参考 Figma 设计稿完成页面实现
- 尽量还原页面结构、层级、间距、圆角、阴影、配色和组件状态
- 优先遵循设计规范，不要随意发挥
- 遇到 Figma 或 `figma.com/make` 链接时，必须通过 Figma MCP 读取 Make/设计内容，不要通过浏览器抓取页面或只凭网页截图猜测
- MCP 读取顺序：先调用 `get_design_context` 获取结构化设计上下文；如果内容过大或被截断，再调用 `get_metadata` 定位具体节点后重新读取；在 MCP 支持的情况下调用 `get_screenshot` 获取视觉参考
- Figma MCP 返回的 React/Tailwind 代码只作为设计结构和视觉信息参考，不能直接当作本项目代码提交
- 实现时必须翻译为微信小程序原生 WXML/WXSS/JS，优先使用微信小程序组件、`wx` API、`rpx` 尺寸和项目本地样式习惯
- MCP 返回可用图片、SVG 或 localhost 资源时，应优先使用这些资源；不要自行编造占位图，也不要为了还原图标或图片额外引入图标包、图片包
- 如设计稿中的部分效果不适合小程序原生实现，应在保证整体风格一致的前提下，采用合理、可落地的实现方式
- 不要为了还原设计而写出过于复杂、难以维护的代码
- 注意处理导航栏、安全区、键盘弹起、滚动区域、文本截断、图片裁剪和长列表性能

---

## 开发原则
- 优先实现真实可运行的页面与流程
- 优先保证每一步改动都可以编译通过
- 不要输出无法运行的半成品
- 不要在页面文件中堆积过多业务逻辑
- 适当拆分 Page、Component、Model、Service、Utils
- 命名清晰，目录结构清晰
- 新增代码时尽量保持与现有项目风格一致
- 优先先打通核心闭环，再补充边角功能

---

## 编码规范
- 保持现有原生小程序写法，使用 `Page`、`Component`、`App`、`wx.*` API
- JavaScript 使用 2 空格缩进，优先使用 `const` / `let`，保持清晰的事件命名和数据字段命名
- WXML 使用小程序模板语法，例如 `wx:if`、`wx:for`、`bindtap`、`bind:xxx`
- WXSS 优先使用 `rpx`，需要兼容安全区时使用小程序可用的安全区方案
- 不要引入 Web DOM、浏览器专属 API、React/Vue/Tailwind 运行时代码
- 网络请求应通过服务层或工具层封装，避免页面内散落重复请求逻辑
- 新增公共能力时优先放入 `utils/`、`services/`、`components/` 等清晰目录，不要过早抽象

---

## 建议目录结构
- `pages/`：页面
- `components/`：公共组件
- `services/`：接口请求与业务服务
- `models/`：类型定义
- `utils/`：工具方法
- `assets/`：静态资源
- `logs/`：改动日志

---

## 交付要求
每次完成任务时，必须满足以下要求：

1. 完成当前要求的实现任务
2. 涉及代码或配置变更时，确保项目可以在微信开发者工具中编译通过；仅文档变更时检查 Markdown 可读性即可
3. 输出“本次改动日志”
4. 将改动日志保存到 `/logs` 文件夹下
5. 日志文件内容需保证可直接阅读，优先使用 `.md` 格式
6. 清理本次任务过程中生成的临时文件，避免在项目目录中残留无用文件

---

## 改动日志要求
每次输出的改动日志应至少包含：
- 本次新增了哪些文件
- 本次修改了哪些文件
- 每个文件的作用
- 本次完成了哪些功能
- 当前仍未完成的内容
- 如有必要，补充运行说明或注意事项

---

## 编码限制
- 不要随意引入新依赖
- 不要修改与当前任务无关的大量代码
- 不要破坏已有可运行功能
- 不要使用与微信小程序运行环境冲突的实现方式
- 不要省略必要的错误处理、空状态、加载状态和鉴权失效处理
- 不要为了追求“高级架构”而增加不必要复杂度

---

## 最终目标
产出一个：
- 符合 Figma 设计稿
- 基于后端 API 文档实现
- 可在微信小程序环境运行
- 可在微信开发者工具中编译通过
- 结构清晰、代码可维护的 AI 陪伴聊天小程序
